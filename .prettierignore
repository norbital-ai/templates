.norbital
.svelte-check
.tmp
node_modules
pnpm-lock.yaml
pnpm-workspace.yaml

# Vendored dependency builds injected by `pnpm yalc:link`. Not ours to format, and
# `prettier --check .` reports ~1100 style warnings on them, which drowns the real ones.
.yalc/
